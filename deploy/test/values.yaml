# Applied after contrib/chart/values.aws-dev.yaml.
agentSandbox:
  # The existing cluster-wide controller watches Sandbox resources in all namespaces.
  enabled: false

console:
  publicUrl: https://centaur-test.theworldnetwork.org

laminarGateway:
  enabled: true
  namespace: laminar-test
  upstream: http://laminar-frontend-service.laminar-test.svc.cluster.local:3000

ingress:
  host: centaur-test.theworldnetwork.org
