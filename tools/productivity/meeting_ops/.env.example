# Full Google authorized-user JSON. The tool never reads this value directly;
# iron-proxy exchanges its refresh token and injects a short-lived bearer token.
GOOGLE_TOKEN_JSON=

# Fixed API-executable Apps Script project. The proxy replaces this path
# placeholder so a caller cannot select another script.
MEETING_OPS_SCRIPT_ID=
